"""Core layer: cross-cutting concerns (constants, config, logging, exceptions).

This layer has NO dependencies on any other application layer. Everything else
may depend on core; core depends on nothing but the standard library.
"""
