# ---- BI TestPilot AI — Runtime dependencies ----

# UI
streamlit==1.39.0

# Data / validation
pandas==2.2.3
openpyxl==3.1.5            # Excel datasource (.xlsx)
numpy==2.1.3

# SQL Server datasource
pyodbc==5.2.0
SQLAlchemy==2.0.36

# Power BI parsing
# PBIX = ZIP container; PBIP/PBIR/TMDL = plain text/JSON — handled with stdlib.
# (No extra heavy deps required for MVP metadata extraction.)

# Screenshot / visual processing
Pillow==10.4.0
# Optional OCR (also requires the Tesseract binary on the machine). The app
# extracts image facts without it and degrades gracefully if it is absent.
# pytesseract==0.3.13

# LLM HTTP calls (provider-agnostic; no vendor SDK lock-in)
requests==2.32.3

# Config / models
pydantic==2.9.2

# Reports
Jinja2==3.1.4
